# creditcard
A project to show the issuing and transaction processes of credit card

After cloning the repo just run using 
mvn clean install 
This is to build the project

Then run 
mvn spring-boot:run

Open http://localhost:8080/
to access the website
